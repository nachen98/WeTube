from pathlib import Path
import pdb, json

pdb.set_trace()
comment_paths=[x for x in Path('/mnt/d/Dropbox/Archive/projects/youtube_crawler/data/').glob('**/*.*') if x.suffix == '.json']

res = []

# for comment_path in comment_paths:
#     res.append({
      
#     })
